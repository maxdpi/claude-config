<!-- /discovery <what to explore> -- open-ended interactive exploration front door. -->
<!-- Routes to the discovery skill: a single-phase frame loop that refuses nothing, -->
<!-- parks after every turn, and never auto-advances. See skills/discovery/SKILL.md. -->
Enter open-ended exploration.

Usage: `/discovery <what you want to explore>` (the argument is optional — without
one, ask the user what they want to explore).

Invoke the `discovery` skill now, passing `$ARGUMENTS` as the exploration topic.
Do not investigate or answer first; the skill IS the exploration loop.
